feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

integrations:
  github:
    url: http://localhost:9099

async:
  maxThreadPoolSize: 12
  timeout: 10
  unit: SECONDS

logging:
  level:
    root: INFO

spring:
  mvc:
    throwExceptionIfNoHandlerFound: true